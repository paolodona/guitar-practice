# Audio bytes never enter git.
songs/*/audio/
songs/*/cache/
capture/
*.wav
*.mp3
*.flac
*.m4a
*.ogg

# Secrets
credentials.json

__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# rb-probe fetches the GPL Rubber Band WASM build; it is not distributed from here.
# fetch-wasm.ps1 reproduces all of these from the pinned, hash-checked npm tarball.
tools/rb-probe/rubberband.wasm
tools/rb-probe/upstream-*
tools/rb-probe/results.json
tools/rb-probe/results-rt.json
tools/rb-probe/.fetch/

# Claude Code: the shared project config is tracked, per-machine overrides are not.
.claude/settings.local.json
